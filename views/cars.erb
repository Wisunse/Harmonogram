<div class="center">

  <div class="users">

    <div class="users-list" layout="column" layout-align="start center">

      <div layout="row" layout-align="center start">
        <div flex="33">ID</div>
        <div flex="33">NAZWA</div>
        <div flex="33"></div>
      </div>

      <div ng-repeat="car in cars.cars track by $index" layout="row" layout-align="center start">

        <div flex> {{$index+1}}</div>
        <div flex>{{car.name}}</div>
        <div flex>
          <md-button class="md-raised md-accent"
                     aria-label="edytuj" ng-click="cars.showEditCar(car, $event)">EDYTUJ</md-button>
        </div>

      </div>
      <div>
        <md-button class="md-primary" ng-click="cars.showAddNewCar($event)">Dodaj nowy samochód</md-button>
      </div>
    </div>

  </div>

</div>